apply plugin: 'groovy'
//apply plugin: 'org.rogern.gradlefmpp'

project.configurations {
    fmpp
}

dependencies {
  compile gradleApi()
  compile localGroovy()
  fmpp fileTree(dir: 'lib', include: '*.jar')
}
